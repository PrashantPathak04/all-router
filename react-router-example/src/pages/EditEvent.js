import PageWrapper from "./PageWrapper";

export default function EditEvent() {
    return (
      <PageWrapper title="Edit Event" subtitle="Update the details of your selected event.">
        <p style={{ color: '#4b5563' }}>
          Edit mode is active. Update the event details and save your changes.
        </p>
      </PageWrapper>
    );
}
